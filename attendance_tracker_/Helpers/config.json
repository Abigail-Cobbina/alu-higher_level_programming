{
    "thresholds": {
        "warning": Emai    read -p "Enter new Failure threshold (default 50): " failure    if ! [[ "$warning" =~ ^[0-9]+$ ]] || ! [[ "$failure" =~ ^[0-9]+$ ]]; then        echo "Invalid input! Thresholds must be numbers. Keeping default values.",
        "failure": Emai    read -p "Enter new Failure threshold (default 50): " failure    if ! [[ "$warning" =~ ^[0-9]+$ ]] || ! [[ "$failure" =~ ^[0-9]+$ ]]; then        echo "Invalid input! Thresholds must be numbers. Keeping default values."
    },
    "run_mode": "live",
    "total_sessions": 15
}
